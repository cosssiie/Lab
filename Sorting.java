public class Sorting {

    // public static void bubbleSortAscending(int[] array) {
    //     int n = array.length;
    //     for (int i = 0; i < n - 1; i++) {
    //         for (int j = 0; j < n - i - 1; j++) {
    //             if (array[j] > array[j + 1]) {
    //                 // swap temp and arr[i]
    //                 int temp = array[j];
    //                 array[j] = array[j + 1];
    //                 array[j + 1] = temp;
    //             }
    //         }
    //     }
    // }

    // public static void bubbleSortDescending(int[] array) {
    //     int n = array.length;
    //     for (int i = 0; i < n - 1; i++) {
    //         for (int j = 0; j < n - i - 1; j++) {
    //             if (array[j] < array[j + 1]) {
    //                 // swap temp and arr[i]
    //                 int temp = array[j];
    //                 array[j] = array[j + 1];
    //                 array[j + 1] = temp;
    //             }
    //         }
    //     }
    // }

    // public static void bubbleSortAscending(Student[] students) {
    //     int n = students.length;
    //     for (int i = 0; i < n - 1; i++) {
    //         for (int j = 0; j < n - i - 1; j++) {
    //             // Викликаємо метод compareTo для порівняння прізвищ студентів
    //             if (students[j].getName().compareTo(students[j + 1].getName()) > 0) {
    //                 // Обмін елементів у масиві
    //                 Student temp = students[j];
    //                 students[j] = students[j + 1];
    //                 students[j + 1] = temp;
    //             }
    //         }
    //     }
    // }
    
    // public static void bubbleSortDescending(Student[] students) {
    //     int n = students.length;
    //     for (int i = 0; i < n - 1; i++) {
    //         for (int j = 0; j < n - i - 1; j++) {
    //             // Викликаємо метод compareTo для порівняння прізвищ студентів
    //             if (students[j].getName().compareTo(students[j + 1].getName()) < 0) {
    //                 // Обмін елементів у масиві
    //                 Student temp = students[j];
    //                 students[j] = students[j + 1];
    //                 students[j + 1] = temp;
    //             }
    //         }
    //     }
    // }
    

    
}
